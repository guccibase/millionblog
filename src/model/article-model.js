class ArticleModel {
  constructor() {
    this.title,
      this.description,
      this.markdown,
      this.createdAt,
      this.slug,
      this.sanitizedHTML;
  }

  toString() {
    return this.name + ", " + this.state + ", " + this.country;
  }
}
